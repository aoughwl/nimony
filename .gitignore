bin/
bin0/
bin1/
bin2/
bin3/
nimcache/
nimblecache/
nimcache_static/
htmldocs/
src/hastur
src/lengc/lengc_grammar.nim
src/nifgram/nifgram
src/lib/nifindexes
tests/tester
*.exe

# Generated by hastur's ensureNiflerParserOverlay: a private copy of the host
# Nim `compiler/` dir with the `concept of` parser fix applied (see hastur.nim).
.nim_overlay/

# Claude Code session/worktree state — must never be committed (its agent
# worktrees otherwise get swept in as bogus submodule gitlinks, breaking
# `git submodule update`).
.claude/
# Scratch build/test logs.
_*.log
